// Prueba Diagnostico

// I Parte: Teoría

// ====================================================================================================

// 1. Respuesta pregunta 1

// a. Number / b. String / c. Boolean / d. undefined

// Ejemplos:

// let number = 234
// let string = 'hola'
// let boolean = false
// let value = undefined

// ====================================================================================================

// 2. Respuesta pregunta 2

// Con un punto
// toyota.modelo = 'yaris'

// Con corchetes
// toyota[precio] = 2000000

// ====================================================================================================

// 3. Respuesta pregunta 3

// El += lo que realiza es la suma de la variable previamente declarada, es lo mismo que redactar:
// list = list + 3234

// primer console.log = 4465
// segundo console.log = '4465TextoDePrueba'

// ====================================================================================================

// 4. Respuesta pregunta 4

// A.
// Caso 1 = En este caso, el operador solamente verifica que el valor de la variable sea diferente a la declaración, independientemente
// de el tipo de dato que este sea, el resultado será true.

// Caso 2 = El operador es estricto, por lo que también toma bajo consideración el tipo de dato que se brinda para la comparación.
// El resultado igualmente es true, debido a que la variable es false, estrictamente diferente a true.

// B.
// Caso 3 = En este caso, al no ser desigualdad estricta, el resultado será false, ya que el programa convierte y compara ambos valores
// y determina que son iguales.

// Caso 4 = Al ser desigualdad estricta, el resultado será true, ya que se realiza la comparación no solo al valor de las variables, sino
// también. al tipo de dato, siendo uno String, y otro Number.

// ====================================================================================================

// 5. Respuesta pregunta 5

// Son aquellas funciones que creamos dentro de la declaración de una variable

// ====================================================================================================

// 6. Respuesta pregunta 6

// Las Function Declaration son aquellas que se crean posteriormente de la palabra function, donde colocamos un nombre a la misma,
// parametros y luego la lógica.

// Las Function Expressions son aquellas que se crean dentro de una variable

// ====================================================================================================

// 7. Respuesta pregunta 7

// Los métodos son funciones que se declaran dentro de los objetos con el fin de realizar un proceso específico dentro de este en el
// momento en que son llamadas

// ====================================================================================================

// 8. Respuesta pregunta 8

// La recursividad es cuando una función se llama a sí misma hasta cumplir una condición específica

// ====================================================================================================

// II Parte: Ejercicios

// 1. Respuesta pregunta 1

/*
function InsertarNumero (array) {
  while (true) {
    let numero = prompt("Digite un número. Escriba 'fin' para terminar.");
    if (numero == 'fin') {
      break;
    } else {
      array.push(parseInt(numero));
    }
  }
}

let ListaNumeros = [];
let ListaPares = [];
let ListaImpares = [];

InsertarNumero (ListaNumeros);

for (i = 0; i < ListaNumeros.length; i++) {
  if (ListaNumeros[i] % 2 === 0) {
    ListaPares.push(ListaNumeros[i])
  } else {
    ListaImpares.push(ListaNumeros[i])
  }
};

console.log(ListaPares, ListaImpares); 
*/

// ====================================================================================================

// 2. Respuesta pregunta 2

// Colocar el código acá

// ====================================================================================================
